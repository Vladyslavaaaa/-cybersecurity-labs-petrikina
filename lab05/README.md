#   Опис програми

Цей проект являє собою інтерактивну програму з графічним інтерфейсом (GUI), створену за допомогою `tkinter` та `cryptography`, для демонстрації механізму **симетричного шифрування** із використанням бібліотеки **Fernet**. Fernet – це стандартний формат, що гарантує **автентифікацію** та **шифрування** даних за допомогою алгоритму **AES-128** у режимі CTR.

Ключова особливість цієї програми — безпечне отримання симетричного ключа зі звичайних персональних даних (пароля) за допомогою функції **PBKDF2HMAC**.

## ✨ Ключові Особливості

* **Надійне Шифрування:** Використовується формат Fernet, що базується на **AES-128** (стандарт індустрії).
* **Отримання Ключа (KDF):** Симетричний ключ генерується не випадково, а з наданих користувачем даних (ПІБ, дата народження, секретне слово) за допомогою алгоритму **PBKDF2HMAC** (Password-Based Key Derivation Function 2) та **SHA-256**.
    * *PBKDF2HMAC забезпечує стійкість до атак перебору, використовуючи високу кількість ітерацій (480,000).*
* **Симетричний Принцип:** Демонструє, що один і той самий ключ необхідний як для шифрування (Відправник), так і для розшифрування (Отримувач).
* **Інтерактивний GUI:** Зручний інтерфейс для покрокової демонстрації: генерація ключа → шифрування → розшифрування.

## 🛠️ Технології та Залежності

* **Python 3.x**
* **`tkinter`:** Для створення графічного інтерфейсу.
* **`cryptography`:** Основна криптографічна бібліотека.
    * **`Fernet`:** Реалізація симетричного шифрування AES-128.
    * **`PBKDF2HMAC`:** Для безпечного отримання ключа з пароля.
 
##  Інструкція Користувача (Покроковий Гайд)
Програма реалізує три основні кроки, які наочно демонструють симетричне шифрування:

Крок 1: Генерація Спільного Ключа (K)
Введіть свої персональні дані (ПІБ, дату, секретне слово) у відповідні поля. Ці дані слугують "паролем" для KDF.

Натисніть "Згенерувати Спільний Ключ".

Програма використає PBKDF2HMAC для створення 32-байтового ключа Fernet (K). Цей ключ є спільним і має бути таємно відомий обом сторонам (Відправнику та Отримувачу).

Крок 2: Шифрування Повідомлення (Відправник)
Введіть текст повідомлення у відповідне поле.

Натисніть "Шифрувати".

Програма зашифрує текст за допомогою ключа K і виведе результат у форматі Base64.

(Для зручності тестування, зашифрований текст автоматично копіюється в поле розшифрування.)

Крок 3: Розшифрування Повідомлення (Отримувач)
Переконайтеся, що в полі "Зашифрований текст" знаходиться зашифроване повідомлення.

Важливо: Отримувач повинен згенерувати точно такий самий ключ K (використовуючи ті ж самі ПІБ, дату та секретне слово).

Натисніть "Розшифрувати".

Результат:

Якщо ключ K збігається, повідомлення буде успішно розшифровано.

Якщо ви зміните хоча б один символ у даних для генерації ключа (Крок 1) і спробуєте розшифрувати, ви отримаєте помилку розшифрування, оскільки ключі не збігатимуться. Це демонструє необхідність спільного секрету.

### Встановлення Залежностей

Перед запуском, треба встановити необхідну бібліотеку:

```bash
pip install cryptography


