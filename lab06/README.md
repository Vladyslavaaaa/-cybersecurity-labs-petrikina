# 🛡️ Демонстрація SQL-ін'єкцій та Методів Захисту

Цей репозиторій містить демонстраційний застосунок, розроблений на Python (Tkinter/SQLite) в рамках лабораторної роботи №6 з дисципліни "**Безпека програм та даних**".

**Мета проекту:** Наочно порівняти поведінку вразливого та захищеного функціоналу пошуку під час спроби атаки SQL-ін’єкцією, демонструючи механізм витоку даних та ефективність захисту.

## ⚙️ Технології та Вимоги

* **Мова:** Python 3.x
* **Інтерфейс:** Tkinter (Графічний інтерфейс)
* **База Даних:** SQLite 3 (Використовується файл `student_search_db.db`)

---

## 🚀 Як запустити програму

1.  **Клонуйте репозиторій** (якщо необхідно).
2.  **Запуск:** Запустіть основний файл програми (`lab07petrikina.py`).
    ```bash
    python ваш_файл.py
    ```
    *(При першому запуску програма автоматично створить базу даних з тестовими записами, включаючи поле `secret_id` для демонстрації витоку.)*

## 💡 Принцип Демонстрації

Застосунок реалізує дві незалежні системи пошуку для прямого порівняння:

| Версія | Принцип Роботи | Метод Захисту |
| :--- | :--- | :--- |
| **1. Вразлива** (ліворуч) | Пряма **конкатенація** рядків: ввід користувача безпосередньо вбудовується у SQL-запит. | **Відсутній** |
| **2. Захищена** (праворуч) | Використання **Параметризованих Запитів (Prepared Statements)**: дані користувача передаються окремо від SQL-коду. | **Параметризовані Запити** |

---

## ⚔️ Сценарій Атаки та Порівняння Результатів

Для демонстрації витоку всіх даних використовується класичний payload, який обходить логіку `WHERE`.

### 1. Вхідний Payload (Ввід для пошуку)
```bash
' OR '1'='1' --
```

### 2. Результати Виконання

| Критерій | 1. ❌ ВРАЗЛИВИЙ ПОШУК | 2. ✅ БЕЗПЕЧНИЙ ПОШУК |
| :--- | :--- | :--- |
| **Виконаний SQL** | Код і дані змішані: `... WHERE name LIKE '%' OR '1'='1' --%'` | Шаблон: `... WHERE name LIKE ?` |
| **Логіка** | Умова `WHERE` стала завжди істинною (`OR '1'='1'`). | Весь payload обробляється як **один literal-рядок**. |
| **Статус** | **🚨 АТАКА УСПІШНА! ВИТІК ДАНИХ (Повертає всі 4 записи).** | **❌ Результатів не знайдено (Атака заблокована).** |
| **Витік Даних** | **Успішний** (Розкрито всі `Secret ID`). | **Невдалий** (Ввід не виконав жодної SQL-команди). |



### 3. Ключовий Висновок

Проект наочно доводить, що **Параметризовані Запити** є ефективним і необхідним стандартом захисту. Вони гарантують, що зловмисний ввід буде оброблений базою даних як **значення**, а не як **виконувана команда**, що запобігає будь-якій SQL-ін'єкції.

