# Опис програми

Цей репозиторій містить програмну реалізацію комплексного двошарового захисту інформації, розроблену в рамках лабораторної роботи №7 з дисципліни "Безпека програм та даних" (Петрікіна В.С.).

Програма поєднує методи симетричного шифрування (AES-256) та стеганографії (LSB) для забезпечення **конфіденційності** та **маскування** секретних даних.

## 🚀 Ключові Особливості

* **Двошаровий Захист:** Послідовне застосування криптографії та стеганографії (Комбінація А).
* **Стійке Шифрування:** Використання алгоритму **AES-256** у режимі CBC (Cipher Block Chaining).
* **Генерація Ключа:** Ключ генерується на основі персональних даних (ПІБ та дата народження) за допомогою функції **PBKDF2HMAC** з SHA-256, що забезпечує високу криптографічну стійкість.
* **Маскування (Стеганографія):** Використання методу **LSB (Least Significant Bit)** для приховування зашифрованого бінарного файлу у зображенні-носії (PNG/BMP).
* **GUI:** Зручний графічний інтерфейс на базі Tkinter для інтуїтивного управління процесами захисту та відновлення.
* **Аналітичний Модуль:** Вивід метрик часу обробки та розміру файлів.

## 💡 Архітектура Захисту (Комбінація А)

### Етап 1: Захист (Encryption + Hiding)

1.  **Генерація Ключа:** Пароль + Сіль $\xrightarrow{\text{PBKDF2-HMAC}}$ Ключ AES-256.
2.  **Шифрування:** Вихідний файл $\xrightarrow{\text{AES-256 CBC}}$ **`encrypted.bin`** (Шифротекст з IV).
3.  **Стеганографія:** `encrypted.bin` + Зображення-носій $\xrightarrow{\text{LSB-Hiding}}$ **`stego_result.png`** (Захищений контейнер).

### Етап 2: Відновлення (Extraction + Decryption)

1.  **Вилучення:** `stego_result.png` $\xrightarrow{\text{LSB-Extraction}}$ **`extracted.bin`**.
2.  **Розшифрування:** `extracted.bin` $\xrightarrow{\text{AES-256 CBC (Ключ)}}$ Відновлений файл.
3.  **Перевірка Цілісності:** Порівняння хешу відновленого файлу з оригінальним.



## 💻 Вимоги та Запуск

### Вимоги

Для запуску програми необхідні наступні бібліотеки Python:

```bash
pip install cryptography Pillow
