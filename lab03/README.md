#  LSB-Steganography-Tool: Власна Реалізація для Приховування Даних у Зображеннях

Цей проект є самостійною реалізацією алгоритму **LSB (Least Significant Bit)**, призначеного для приховування текстової інформації всередині зображень-контейнерів. Програма забезпечує **високу візуальну непомітність** прихованого повідомлення.

---

## 🌟 Принципи Роботи

Програма використовує метод LSB, який замінює **найменш значущі біти** (останній біт) у кожній колірній складовій (**R, G, B**) пікселя на біти секретного повідомлення. Оскільки зміна відбувається лише в останньому біті, візуально стегоконтейнер залишається **ідентичним** оригінальному зображенню. 

### 🔑 Ключові Технічні Деталі

* **LSB-Маніпуляція:** Використовуються **побітові операції** (`&` та `|`) для ефективного керування молодшими бітами.
* **Механізм Надійного Декодування:** Для забезпечення коректного вилучення повідомлення перед ним кодується **префікс довжини** (4 байти, 32 біти). Це дозволяє декодеру точно знати, де закінчується прихований текст.
* **Вимога до Контейнера:** Використовуються лише **формати без втрат** (як **PNG** або **BMP**), оскільки стиснення JPEG призводить до втрати LSB-бітів і руйнування прихованого повідомлення.
* **Аналіз Місткості:** Програма автоматично розраховує та відображає максимальну кількість бітів, яку може вмістити зображення, і порівнює її з обсягом секретного повідомлення.

---

## 🚀 Вимоги та Встановлення

Для запуску програми потрібен встановлений **Python 3** та бібліотека **Pillow (PIL)**.

```bash
# Встановлення необхідних бібліотек
pip install Pillow
