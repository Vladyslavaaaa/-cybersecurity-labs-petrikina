###Опис програми 

Цей проект являє собою навчальну інтерактивну програму з графічним інтерфейсом (GUI), створену за допомогою `tkinter` та `hashlib`, для демонстрації базових механізмів створення та перевірки **цифрових підписів**.

Програма використовує спрощений, але функціональний криптографічний підхід, де цифровий підпис генерується шляхом **XOR-операції** між **хешем документа (H)** та **приватним ключем (PR)**.

## ✨ Ключові Особливості

* **Генерація Ключів:** Приватний та Публічний ключі генеруються на основі вхідних даних користувача (Ім'я, Дата, Секретне слово) за допомогою алгоритму **SHA-256**.
* **Створення Підпису:** Підпис $S = H \oplus PR$, де $H$ — SHA-256 хеш документа.
* **Перевірка Підпису:** Перевірка здійснюється шляхом відновлення хешу документа: $H_{відновлений} = S \oplus PR$. Якщо $H_{відновлений}$ збігається з новим хешем документа $H_{новий}$, підпис вважається дійсним.
* **Інтерактивний GUI:** Зручний інтерфейс дозволяє крок за кроком виконувати генерацію ключів, підписання документа та перевірку, наочно демонструючи принцип **цілісності** та **автентичності**.

---

## 🛠️ Технології

* **Python 3.x**
* **`hashlib`:** Для реалізації функції хешування SHA-256.
* **`tkinter`:** Для створення графічного інтерфейсу користувача.

---
## 🚀 Встановлення та Запуск

Оскільки програма використовує лише стандартні бібліотеки Python (hashlib, tkinter), додаткові залежності не потрібні.
```bash
lab04petrikina.py
